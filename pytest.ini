[pytest]
addopts = -ra

markers =
    postgres: tests that require the Postgres test container
    liquibase: tests that depend on Liquibase-applied schema
    integration: integration tests that may require external services
