# ----------------------------------------------------------------------
# Test Database Connection Settings (Used by SQLAlchemy Only)
# ----------------------------------------------------------------------

TEST_DB_HOST=localhost
TEST_DB_PORT=25432
TEST_DB_NAME=my_entity_service
TEST_DB_USER=my_entity_app
TEST_DB_PASSWORD=my_entity_app_password

# ----------------------------------------------------------------------
# Liquibase Test Properties File
# This is the filename inside migrations/liquibase/
# Example: docker-liquibase.test.properties
# ----------------------------------------------------------------------

TEST_LIQUIBASE_PROPERTY_FILE=test-liquibase.properties

# ----------------------------------------------------------------------
# Optional: Pytest Logging Overrides
# Enable with pytest -o log_cli=1 if needed
# ----------------------------------------------------------------------
# PYTEST_ADDOPTS=--log-cli-level=INFO